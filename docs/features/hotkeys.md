# Global shortcut

Cliiippo has one optional global shortcut that opens clipboard history directly. The recorder accepts
ordinary macOS key combinations requiring Command, Option, or Control, plus standalone function keys.
It does not support double-tap modifiers or a Hyper key.

The binding is stored under `hotkey.toggleClipboard` in the current bundle's defaults. No shortcut
is assigned on a fresh install.
